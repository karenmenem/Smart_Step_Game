# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=smart_step_learning

# JWT Secret (change this to a secure random string in production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Server Configuration
PORT=5001
FRONTEND_URL=http://localhost:3000

# File Upload Configuration
MAX_FILE_SIZE=5242880

# ============================================
# Arduino Quiz Buzzer Configuration (OPTIONAL)
# ============================================
# Set to 'true' to enable Arduino integration
# Set to 'false' or remove to disable
ARDUINO_ENABLED=false

# Arduino serial port - find this in Arduino IDE: Tools > Port
# Mac: /dev/cu.usbmodem... or /dev/tty.usbmodem...
# Windows: COM3, COM4, etc.
# Linux: /dev/ttyACM0, /dev/ttyUSB0, etc.
ARDUINO_PORT=/dev/cu.usbmodem11301

# ============================================
# How to use Arduino:
# 1. Upload quiz_buzzer.ino to your Arduino board
# 2. Close Serial Monitor in Arduino IDE
# 3. Set ARDUINO_ENABLED=true above
# 4. Set ARDUINO_PORT to match your port (check Arduino IDE)
# 5. Restart the backend server
# 6. Physical buttons will simulate A, B, C, D keypresses
#
# To disable: Set ARDUINO_ENABLED=false or comment it out
# The server works perfectly without Arduino!
# ============================================
